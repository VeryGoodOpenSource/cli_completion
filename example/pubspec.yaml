name: example
description: Example for cli_completion
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  args: ^2.3.1
  cli_completion:
    path: ../
  mason_logger: ^0.2.2

dev_dependencies:
  mocktail: ^1.0.0
  test: ^1.25.0
  very_good_analysis: ^5.1.0

executables:
  example_cli: